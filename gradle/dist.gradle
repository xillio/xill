apply plugin: 'com.google.osdetector'
task zipDist(type: Zip, group: 'build', description: 'Build zip distribution') {
    dependsOn prepare
    dependsOn build

    classifier osdetector.classifier
    mustRunAfter 'jfxDeploy'
    baseName project.appID
}

buildDists.dependsOn zipDist