<?xml version="1.0" encoding="utf-8"?>
<function>
    <description>
This browser function enters the string into the form input-element.  
  
This is useful for entering text into web forms before submitting them.
    </description>
    <examples>
        <example title="Usage">
            <header>Enter text into a text input element.</header>
            <code>
                use Web;
                use System;

                var page = Web.loadPage ("http://www.xillio.com/");
                var text = "migration";

                if (page != null) {
                    var searchbox = Web.xPath (page, "//input[@name='email']");
                    System.print(Web.getText(searchbox));
                    Web.input(searchbox, text);  // actually writes text into the searchbox as if a user were typing
                    System.print(Web.getText(searchbox));
                } else {
                    System.print("no page");
                }
                /*

                This will output:
                INFO
                INFO    migration

                */
            </code>
        </example>
    </examples>
    <references>
        <reference>xPath</reference>
        <reference>getText</reference>
    </references>
    <tags>
        load, page, web, text, input
    </tags>
</function>