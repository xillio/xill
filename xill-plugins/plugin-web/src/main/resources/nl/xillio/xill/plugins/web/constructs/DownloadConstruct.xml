<?xml version="1.0" encoding="utf-8"?>
<function>
    <description>
This function downloads a content from the link specified by the URL and save it to a file.  

Optional webcontext parameter can be used for doing a download in a existing web context.  
The download function supports both HTTP and HTTPS protocol and automatically bypasses all invalid or not trusted SSL certificate in case of HTTPS.  
Default timeout if not provided is 5000 ms.
    </description>
    <examples>
        <example title="Usage">
            <header>Download a pdf document</header>
            <code>
                use Web;
                Web.download("http://www.something.com/archive/document.pdf", "c:/tmp/doc.pdf", null, 60000);
            </code>
        </example>
    </examples>
    <references>
        <reference>loadPage</reference>
    </references>
    <tags>
        download, page, web, url, file
    </tags>
</function>