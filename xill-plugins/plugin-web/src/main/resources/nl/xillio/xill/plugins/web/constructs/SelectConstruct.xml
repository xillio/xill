<?xml version="1.0" encoding="utf-8"?>
<function>
    <description>
        In an HTML-select box, executing ``select`` marks the option ``node`` as selected . If the boolean parameter
        ``select`` equals true, it selects the option, if false then it's deselected.

        **NOTE**: The deselection doesn't work for ``option`` tag.
    </description>
    <examples>
        <example title="Usage">
            <header>Select a radio button.</header>
            <code>
                use Web;
                use System;

                var page = Web.loadPage("http://www.w3schools.com/html/html_form_input_types.asp");
                var input = Web.xPath(page, "//input[@type='radio']");
                System.print(input);
                var choice = Web.xPath(page, "//input[@type='radio'][2]");
                var before = Web.selected(choice);
                Web.select(choice, true);
                var after = Web.selected(choice);
                System.print(before);
                System.print(after);
            </code>
        </example>
    </examples>
    <references>
        <reference>select</reference>
        <reference>selected</reference>
        <reference>loadPage</reference>
        <reference>xPath</reference>
    </references>
    <tags>
        load, page, web, xpath, select
    </tags>
</function>