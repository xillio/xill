<?xml version="1.0" encoding="utf-8"?>
<function>
    <description>
This browser function sets the focus to the selected node.  

This is used as a replacement for mouseover and mouseout events in the absence of a mouse.  
Some pages require these events to be triggered to load data for combo-boxes, menus or other dynamic elements.
    </description>
    <examples>
        <example title="Usage">
            <header>Set focus on an element on a web page.</header>
            <code>
                use Web;
                use System;

                var page = Web.loadPage ("http://www.xillio.com/");

                if (page != null) {
                    var node = Web.xPath(page, "//input[@name='q']"); // select the search box at the top of the Xillio homepage
                    Web.focus(node); // set focus to that box
                } else {
                    ("no page");
                }
            </code>
        </example>
    </examples>
    <references>
        <reference>loadPage</reference>
        <reference>xPath</reference>
    </references>
    <tags>
        load, page, web, focus
    </tags>
</function>