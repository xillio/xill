<?xml version="1.0" encoding="utf-8"?>
<function>
    <description>
        Base-64 decode the string content, and store it in the file fileName.
        Commonly used to decode binary data transferred over a text channel (such as attachments in e-mails).
    </description>
    <examples>
        <example title="Usage">
            <p>Assuming we have a file 'node.xml' containing 'Hello World'</p>
            <code>
                String.base64Encode("C:/tmp/note.xml");

                // Outputs: SGVsbG8gV29ybGQ=
            </code>
        </example>
    </examples>
    <references>
        <reference>base64Encode</reference>
    </references>
    <searchTags>
        Base64, encode
    </searchTags>
</function>