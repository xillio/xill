<?xml version="1.0" encoding="utf-8"?>
<function>
  
  <description>
	 Returns a boolean if the string text matches the pattern specified by the regex.
	 Optionally a timeout can be provided in seconds, the default value is 5 seconds. 
	 Set timeout to zero to let the function run without timeout. This should only be altered when dealing with large source documents. 
	 The timeout is there to protect the system from hanging on invalid expressions.
	 See the Regular Expressions page for more about Regular Expression support in Xillio Content Tools.
  </description>
  <examples>
    <example description="The following all return true:"><![CDATA[
    	String.matches("I need help!", ".*help.*");
		String.matches("<p>paragraph text</p>", "<p\\b[^>]*>(.*?)</p>");
		String.matches("No", "(?i)no
n");
    ]]></example>
    <example description="The following all return false:"><![CDATA[
    	String.matches("I need help!", "help.*");
		String.matches("<p>paragraph text</p> more text", "<p\\b[^>]*>(.*?)</p>");
		String.matches("No", "no
n");
		]]>
    </example>
    <example description="One application could be:"><![CDATA[
    var testUrl = "http://www.xillio.com/test.png";
	if (!String.matches(testUrl, ".*\\.(pdf
doc
docx
ppt
pptx
xls
xlsx
mp3
mp4
wmv
flv
flash
ogg
zip
png
bmp
jpg
jpeg
gif
tif
tiff
txt)")) {
    // Page
    System.print("Found page with url: " + testUrl);
	} else {
    // Binary
    System.print("Found binary file with url: " + testUrl);
	}
    ]]>
    </example>
  </examples>
  
   <searchTags>
	<searchTag>
	regex
	</searchTag>
	<searchTag>
	match
	</searchTag>
  </searchTags>
</function>