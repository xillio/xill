<?xml version="1.0" encoding="UTF-8"?>
<function>
  <description>
Connect to a sqlite database. Returns a connection object.  

The file parameter is optional. An in-memory database will be created when it is left empty.
  </description>
  <examples>
    <example>
      <header>Connect to an in memory sqlite database
      </header>
      <code>
	
	var connection = Database.sqliteConnect();
	
      </code>
    </example> 
     <example>
      <header>Connect to a sqlite database
      </header>
      <code>
	
	var connection = Database.sqliteConnect("name");
	
      </code>
    </example> 
  </examples>
  <references>
    <reference>connect</reference>
    <reference>mysqlConnect</reference>
    <reference>oracleConnect</reference>
    <reference>mssqlConnect</reference>
  </references>
  <tags>
    sqlite,lite, connect
  </tags>
</function>