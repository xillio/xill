<?xml version="1.0" encoding="utf-8"?>
<function>
    <description>
Carries out the Head REST request. Returns response from server.

The response can be one of the following types:
* XML (XmlNode)
* JSON (Object)
* String  

Options:
* timeout - processing timeout in ms (default = 5000)
* proxyhost - proxy server URL
* proxyport - port of the proxy server
* proxyuser - user name for proxy valdiation         
* proxypass - password for proxy valdiation

The proxy options can be combined as follows:
proxyhost
proxyhost+proxyport 
proxyhost+proxyport+proxyuser+proxypass
proxyuser+proxypass
    </description>
    <examples>
        <example title="Usage">
            <header>Head request</header>
            <code>
use System;
use REST;
var response = REST.head("http://whatever.com/uri", {"timeout" : 3000});
System.print(response);
</code>
        </example>
    </examples>
    <references>
        <reference>delete</reference>
        <reference>get</reference>
        <reference>post</reference>
        <reference>put</reference>
    </references>
	    <tags>
        REST, head, request, response, JSON
    </tags>
</function>