<?xml version="1.0" encoding="utf-8"?>
<function>
	<description>
		Persists a content type and all its decorators that have previously been loaded by Document.loadContentTypes or Document.loadDecoratorDefinitions to MongoDB.
                After persiting these content types will be loaded on startup. 
                Assumes that MongoDB is running. 
	</description>
	<examples>
		<example title="Usage">
			<header>Persisting a content type</header>
			<code>
				use Document;
				
				Document.persistContentType("myContentType");
			</code>
		</example>
	</examples>
	<searchTags>
		get, database, db, udm, document
	</searchTags>
</function>