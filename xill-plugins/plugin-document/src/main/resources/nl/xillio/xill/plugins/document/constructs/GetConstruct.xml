<?xml version="1.0" encoding="utf-8"?>
<function>
	<description>
		Gets a document from the database.
		Valid values for the version are "current" or a number, and valid values for the section are "source" and "target".
		Throws an exception if the given id or version does not exist, or if the section is invalid.
	</description>
	<examples>
		<example title="Usage">
			<header>Getting a document.</header>
			<code>
				use Document;
				
				// Given that the ID of the document is stored in the variable 'id'.
				var doc = Document.get(id);
			</code>
			<header>Getting a specific version and section from a document.</header>
			<code>
				use Document;
				
				// Given that the ID of the document is stored in the variable 'id'.
				var version = "2";
				var section = "source";
				var doc = Document.get(id, version, section);
			</code>
		</example>
	</examples>
	<searchTags>
		get, database, db, udm, document
	</searchTags>
</function>