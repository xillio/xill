<?xml version="1.0" encoding="utf-8"?>
<function>
    <description>
Renames an existing collection.
A robot exception will be generated if you provide a `target` that is the name of an existing collection and `dropTarget` is false, or if `collection` is the name of a collection that doesn't exist.
If `dropTarget` is true, mongod drops the target collection prior to renaming the collection.
    </description>
    <examples>
        <example title="Usage">
            <header>Rename the "asset" collection to "assets"</header>
            <code>
                use Mongo;
                use System;
                Mongo.connect("udm_default");

                Mongo.renameCollection("asset", "assets", true);
            </code>
        </example>
    </examples>
    <references>
        <reference>connect</reference>
    </references>
    <tags>
        mongodb, rename
    </tags>
</function>
