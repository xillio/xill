<?xml version="1.0" encoding="utf-8"?>
<function>
    <description>
	Removes documents from the database based on a filter. The filter can be built according to the MongoDB 3.0 Query API.
        If no version is provided the whole document entry will be removed. Otherwise only the particular revision will be removed.
        Note! It is not possible to remove the current version. To do this you should use update.
    </description>
    <examples>
        <example title="Usage">
            <header>Removing a full content type.</header>
            <code>
                use Document;
                var doc = Document.removeWhere({"contenttype" : "Agenda"});
            </code>
            <header>Getting a specific version from document created by a user.</header>
            <code>
                use Document;

		// Filter should comply to http://docs.mongodb.org/manual/reference/operator/query/
                var filter = {
                  "source.modifiedBy": "Jack"
                };
                var version = "2";
                var section = "source";
                var doc = Document.removeWhere(filter, version, section);
            </code>
        </example>
    </examples>
    <searchTags>
        delete, remove, database, db, udm, document
    </searchTags>
    <references>
        <refence>update</refence>
    </references>
</function>