<?xml version="1.0" encoding="utf-8"?>
<function>
    <description>
Find all documents that match a filter and/or a sort query.

This construct expects a mongo filter and a mongo sort query. It will iterate over all documents that match that filter.
It will then be sorted afterwards. If no filter is provided this construct will iterate over all results.
If no sort query is provided this construct will iterate without sorting it.
    </description>
    <examples>
        <example title="Usage">

            <header>Iterate over all CustomPages</header>
            <code>
                use Document;
                use System;

                // Run the query
                var filterObj = {"contenttype": "CustomPage"};
                var sortObj = {"_id": 1};

                var pages = Document.find(filterObj, sortObj);

                foreach(page in pages) {
                    System.print(page._id);
                }
            </code>
        </example>
    </examples>
    <references>
        <reference>get</reference>
        <reference>save</reference>
        <reference>deleteWhere</reference>
    </references>
    <tags>
        udm, define, build, create, remove, bulk, filter, sort
    </tags>
</function>
