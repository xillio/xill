<?xml version="1.0" encoding="utf-8"?>
<function>
    <description>
        Search for documents based on a mongodb filter.
        By default the returned documents come from the target section and represent the current version. This behaviour
        can be overridden by providing the version and section parameters.
    </description>
    <examples>
        <example title="Usage">
            <header>Getting all documents with the Agenda type.</header>
            <code>
                use Document;

                // Given that the ID of the document is stored in the variable 'id'.
                var filter = {
                    "contenttype": "Agenda"
                };
                var docs = Document.findWhere(filter);
                foreach(document in docs) {
                }
            </code>
            <header>Getting all documents that don't contain a tridion component field yet.</header>
            <code>
                use Document;

                var filter = {
                    "target.current.tridion.component": {
                        "$exists" : false
                    }
                };

                var docs = Document.findWhere(filter);
                foreach(document in docs) {
                }
            </code>
        </example>
    </examples>
    <searchTags>
        find, database, db, udm, document
    </searchTags>
</function>