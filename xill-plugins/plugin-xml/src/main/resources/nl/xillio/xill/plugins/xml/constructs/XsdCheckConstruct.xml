<?xml version="1.0" encoding="utf-8"?>
<function>
    <description>
        Validates a xml file against a xsd file. Returns true if the xml file is valid, returns false and a warning when it is not.
    </description>
    <examples>
        <example title="Usage">
            <header>Validating a xml file against a xsd.</header>
            <code>
                use XML;
                use System;

                if (XML.xsdCheck("d:/temp/books.xml", "d:/temp/books.xsd")) {
                    System.print("File is valid!");
                }
            </code>
        </example>
    </examples>
    <references>
        <reference>xPath</reference>
    </references>
	    <tags>
        XSD, check, validate, XML
    </tags>
</function>