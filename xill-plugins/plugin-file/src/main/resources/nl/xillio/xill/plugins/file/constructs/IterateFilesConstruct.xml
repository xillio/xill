<?xml version="1.0" encoding="utf-8"?>
<function>
    <parameters>
        <param name="uri"/>
        <param name="recursive" default="false"/>
    </parameters>
    <description>
        <![CDATA[Iterates over all files in a folder. If recursive is set to true all files in sub folders will also be iterated]]></description>
    <examples>
        <example description="Print all files in a folder:"><![CDATA[
        use File;
        use System;
        var iterator = File.iterateFiles("../files", true);
        foreach(file in iterator) {
            System.print(file);
        }
    ]]></example>
    </examples>
    <references>
        <reference package="File">iterateFolders</reference>
    </references>
    <searchTags>
    </searchTags>
</function>