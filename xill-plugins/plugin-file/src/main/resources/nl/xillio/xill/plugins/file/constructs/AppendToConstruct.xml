<?xml version="1.0" encoding="utf-8"?>
<function>
    <parameters>
        <param name="uri"/>
        <param name="content"/>
    </parameters>
    <description>
        <![CDATA[Appends content in the form of text to a file, creating it if it doesn't exist. Returns the absolute path to the file.]]></description>
    <examples>
        <example title="Usage">
            <header>Appending a string to a file:</header>
            <code>
                use File;
                File.appendTo("file.txt", "Add this line");
            </code>
        </example>
    </examples>
    <references>
        <reference>saveTo</reference>
    </references>
    <searchTags>
        write, text, new, create, append, add
    </searchTags>
</function>