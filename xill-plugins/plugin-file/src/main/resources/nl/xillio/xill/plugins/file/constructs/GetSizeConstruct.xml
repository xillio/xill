<?xml version="1.0" encoding="utf-8"?>
<function>
    <description>
Returns the size of a file or folder in bytes.  

The uri can be relative or absolute.  
When using a relative uri, its starting point is the containing folder of the robot.
	</description>
    <examples>
        <example title="Usage">
            <header>Get the size of a folder</header>
            <code>
                use File;
                var size = File.getSize("C:/Users/Me");
            </code>
			<header>Get the size of a robot in the same folder</header>
            <code>
                use File;
                var size = File.getSize("SiblingRobot.xill");
            </code>
        </example>
    </examples>
</function>