-------
loadxml
-------

**loadxml (**\ *string* **uri)**

Description
-----------

Loads the local XML file ``uri`` and returns an XML object.

See also :doc:`stringtoxml`

Example
-------

-   
    
    Assuming we have an XML file ``note.xml`` which contains the following:
    
    ::

        <note id="1">
            <to>Tove</to>
            <from>Jani</from>
            <heading>Reminder</heading>
            <comment>This was sent to James as well</comment>
            <comment>John has not responded to the invitation</comment>
            <body>Don't forget me this weekend!</body>
        </note>
        
    Then executing the following code:
    
    ::
    
        note = loadxml ("C:/tmp/note.xml");

        comments = xpath(note, "//comment/text()");

        foreach (_, comment, comments) {
            log (comment);
        }        

    will print:
    
    ::
    
        This was sent to James as well
        John has not responded to the invitation

Tags
----

:doc:`_tag_xmlhtml`