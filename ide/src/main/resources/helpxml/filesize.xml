<?xml version="1.0" encoding="utf-8"?>
<function Tag="filesize">
  <name>filesize</name>
  <parameters>
    <param name="uri">
      <type>string</type>
    </param>
  </parameters>
  <description><![CDATA[Returns the size of the file uri on the local computer. Returns 0 if the file does not exist or is a directory.]]></description>
  <examples>
    <example description="The following logs the size of the file download.png if it exists and is not a directory:"><![CDATA[downloadlocation = "C:/tmp/download.png";
size = 0;
]]></example>
    <example description="if (fileexists (downloadlocation)) {"><![CDATA[    size = filesize(downloadlocation);
    if (size == 0) {
        log (downloadlocation + " is a directory.");
    }
} else {
    log ("file not found: " + downloadlocation);
}
log (size);
]]></example>
  </examples>
  <Applications />
  <tags>
    <tag> fileinfo</tag>
    <tag>File</tag>
  </tags>
</function>