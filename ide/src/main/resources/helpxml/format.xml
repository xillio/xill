<?xml version="1.0" encoding="utf-8"?>
<function Tag="format">
  <name>format</name>
  <parameters>
    <param name="text,">
      <type>string</type>
    </param>
    <param name="values">
      <type>any</type>
    </param>
  </parameters>
  <description><![CDATA[Returns a string which contains text with values inserted at certain placeholders in a certain format, just like a c-style printf statement.DetailsThe first parameter, text, is a format string specifying how the objects in the second parameter, values, are to be formatted. The format string contains plain text as well as format specifiers, which are special characters that format the arguments of values.values accepts a single parameter. When the format string expects multiple parameters these have to be supplied as a list, surrounded by braces ‘{}’ (see the ‘date’ example below).Format specifiers begin with a percent sign (%) and end with a converter. The converter is a character indicating the type of argument to be formatted. In between the percent sign (%) and the converter you can have optional flags and specifiers.Some converters and flags used in formatstrings:ConverterFlagExplanationd A decimal integer.f A float.n A new line character appropriate to the platform running the application. You should always use %n, rather than n.tB A date & time conversion locale-specific full name of month.td, te A date & time conversion 2-digit day of month. td has leading zeroes as needed, te does not.ty, tY A date & time conversion ty = 2-digit year, tY = 4-digit year.tl A date & time conversion hour in 12-hour clock.tM A date & time conversion minutes in 2 digits, with leading zeroes as necessary.tp A date & time conversion locale-specific am/pm (lower case).tm A date & time conversion months in 2 digits, with leading zeroes as necessary.tD A date & time conversion date as %tm%td%ty08Eight characters in width, with leading zeroes as necessary.+Includes sign, whether positive or negative.,Includes locale-specific grouping characters.-Left-justified...3Three places after decimal point.10.3Ten characters in width, right justified, with three places after decimal point.For more documentation see: http://docs.oracle.com/javase/tutorial/java/data/numberformat.html and http://docs.oracle.com/javase/1.5.0/docs/api/java/util/Formatter.html#syntax]]></description>
  <examples>
    <example description="basic example:"><![CDATA[i = 461012;
log(format("The value of i is: %d%n", i));
]]></example>
    <example description="The %d specifies that the single variable is a decimal integer. The %n is a platform-independent newline character. The output is:"><![CDATA[The value of i is: 461012
]]></example>
    <example description="Date example:"><![CDATA[log(format("Today is: %td %tB %tY", {date(), date(), date()}));
]]></example>
    <example description="The example supplies three time the current date to the format function in order to extract desired day, month and year. The output will be something like:"><![CDATA[Today is: 03 april 2015
]]></example>
    <example description="More examples (note that the floating point decimal separator is dependent on the system locale):"><![CDATA[n = 461012;
pi = 3.141593;
]]></example>
    <example description="log (format(&quot;%d%n&quot;, n));      //  --&gt;  &quot;461012&quot;"><![CDATA[log (format("%08d%n", n));    //  -->  "00461012"
log (format("%+8d%n", n));    //  -->  " +461012"
log (format("%,8d%n", n));    // -->  " 461,012"
log (format("%+,8d%n%n", n)); //  -->  "+461,012"
]]></example>
    <example description="log (format(&quot;%f%n&quot;, pi));       // --&gt;  &quot;3,141593&quot;"><![CDATA[log (format("%.3f%n", pi));     // -->  "3,142"
log (format("%10.3f%n", pi));   // -->  "     3,142"
log (format("%-10.3f%n", pi));  // -->  "3,142"
]]></example>
  </examples>
  <Applications />
  <tags>
    <tag>Text</tag>
  </tags>
</function>