<?xml version="1.0" encoding="utf-8"?>
<function Tag="loadpage">
  <name>loadpage</name>
  <parameters>
    <param name="url">
      <type>string</type>
    </param>
  </parameters>
  <description><![CDATA[Loads the webpage found at url and returns it as a page object. If loading fails the page object will contain an error code, which can be accessed using the function geterror. Also note that it is possible and common to read files by calling loadpage with a url starting with file:// as the parameter. Usually it is a good idea to reduce loading time by setting up the webclient({"enablecss"=false, "enablejs"=false}) before calling loadpage. This deactivates Javascript and CSS and dramatically reduces long waits for script timeouts.]]></description>
  <examples>
    <example description="Loop through menu on a webpage and store all navigation items:"><![CDATA[webclient({"enablecss"=false, "enablejs"=false});
]]></example>
    <example description="homeurl = &quot;http://www.xillio.com/&quot;;"><![CDATA[]]></example>
    <example description="page = loadpage (homeurl);"><![CDATA[mainmenutitles = xpath (page, "//ul[@class='active-branch']/li/a/text()");
mainmenuurls = xpath (page, "//ul[@class='active-branch']/li/a/@href");
]]></example>
    <example description="foreach (numberinlevel, mainmenuurl, mainmenuurls) {"><![CDATA[    log(mainmenutitles[numberinlevel] + " (" + mainmenuurls[numberinlevel] + ")");
}
]]></example>
  </examples>
  <Applications />
  <tags>
    <tag>Browser</tag>
  </tags>
</function>