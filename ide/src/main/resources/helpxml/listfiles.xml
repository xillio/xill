<?xml version="1.0" encoding="utf-8"?>
<function Tag="listfiles">
  <name>listfiles</name>
  <parameters>
    <param name="uri,">
      <type>string</type>
    </param>
    <param name="recursive ">
      <type>boolean</type>
    </param>
  </parameters>
  <description><![CDATA[Returns an iterable containing the absolute file names in the folder specified by uri. Returns empty if uri is empty or not a folder. If the optional parameter recursive is set to true, this function will also return the files in all subfolders of uri.]]></description>
  <examples>
    <example description="Looping through all files in a directory (in this case stored webpages):"><![CDATA[cmsdirectory =  "C:/tmp/test/html-cms/";
cmspages = listfiles (cmsdirectory);
]]></example>
    <example description="foreach(_, cmsfile, cmspages) {"><![CDATA[    cmspage = trim (loaddata(cmsfile), true);
    if (cmspage != null) {
        log ("Successfully read page from: " + cmsfile);
    }
}
]]></example>
    <example description="There is a nice trick for getting the number of files in a directory:"><![CDATA[files = listfiles ("C:/tmp/"); // the files variable now contains the file list
number = length(files);
log (number);
]]></example>
    <example description="This will print the number of files in the given directory. Subdirectories do not count as files."><![CDATA[]]></example>
  </examples>
  <Applications />
  <tags>
    <tag> listfolders</tag>
    <tag>File</tag>
  </tags>
</function>