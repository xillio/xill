<?xml version="1.0" encoding="utf-8"?>
<function>
    <description>
Returns a randomized value based on the optional parameter value.  

If an integer argument N is provided the return value is a random integer between 0 (inclusive) and N (exclusive).
If a list argument is provided, the result is a random entry from the list.
If no parameter or a negative number is provided, a random floating point number between 0 and 1 will be generated.
    </description>
    <examples>
        <example title="Get a number between 1 and 6 (inclusive)">
            <code>
                use Math;

                // Note that we add 1 to the result, to get a number between 1 and 6 inclusive.
                Math.random(6) + 1;
            </code>
        </example>
        <example title="Get a random item from a list">
            <code>
                use Math;

                var list = ["monday", "tuesday", "wednesday", "thursday", "friday"];
                Math.random(list);
            </code>
        </example>
        <example title="Get a random number between 0 and 1">
            <code>
                use Math;

                Math.random();
            </code>
        </example>
    </examples>
    <searchTags>
        random
    </searchTags>
</function>