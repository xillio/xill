<?xml version="1.0" encoding="utf-8"?>
<function>
    <description>
Copy a file or folder to a target destination, overwriting it if it exists.  

Returns an object containing the source destination [from] and the target destination [into].
</description>
    <examples>
        <example title="Usage">
            <header>Copy a file</header>
            <code>
                use System;
                use File;
                var result = File.copy("file.txt", "file_copy.txt");

                // Prints {"from": "absolute/path/to/file.txt", "into": "absolute/path/to/file_copy.txt"}
                System.print(result);
            </code>
        </example>
    </examples>
    <tags>
        test, other
    </tags>
</function>