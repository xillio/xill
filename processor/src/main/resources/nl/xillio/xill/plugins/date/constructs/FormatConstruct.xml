<?xml version="1.0" encoding="utf-8"?>
<function>
    <description>
Converts the date to a string using the provided format.  
 
If no format is given the pattern "yyyy-MM-dd HH:mm:ss" is used.

| Symbol | Meaning                  | Presentation  | Example                |
| ------ | --------                 | ------------- | ---------------------- |
| u/y    | year                     | year          | 2004; 04               |
| Q/q    | quarter-of-year          | number/text   | 3; 03; Q3; 3rd quarter |
| M/L    | month-of-year            | number/text   | 7; 07; Jul; July; J    |
| d      | day-of-month             | number        | 10                     |
| E      | day-of-week              | text          | Tue; Tuesday; T        |
| H      | hour-of-day              | number        | 14                     |
| m      | minute-of-hour           | number        | 55                     |
| s      | second-of-minute         | number        | 30                     |
| a      | am/pm                    | text          | PM;                    |
| '      | escape for text ('text') | delimiter     |                        |
| ''     | literal '                | literal       | '                      |

Apart from the characters ' [ ] # { } no other special UTF-8 character has functionality in the format and could be used freely.

Text: The text style is determined based on the number of pattern letters used. Less than 4 pattern letters will use the short form. Exactly 4 pattern letters will use the full form. Exactly 5 pattern letters will use the narrow form.

Number: If the count of letters is one, then the value is output using the minimum number of digits and without padding. Otherwise, the count of digits is used as the width of the output field, with the value zero-padded as necessary. Some patterns have exceptional behavior however.

Number/Text: If the count of pattern letters is 3 or greater, use the Text rules above. Otherwise use the Number rules above.

For more information and use see: [Java's pattern definition](https://docs.oracle.com/javase/8/docs/api/java/time/format/DateTimeFormatter.html#patterns).

    </description>
    <examples>
        <example title="Usage">
            <header>Showing formated current datetime.</header>
            <code>
use System, Date;
var date = Date.now();
System.print(Date.format(date));
            </code>
            <header>A more complex format</header>
            <code>
use System, Date;
var date = Date.now();
var format = Date.format(date, "QQQ:yyyy E dd MMMM H'h'mm\"ss''");
System.print(format);
// Results in the form:
// "Q1:2016 Thu 31 March 10h59"39'"
            </code>
        </example>
    </examples>
    <references>
        <reference>info</reference>
    </references>
	    <tags>
        date, time, datetime, format
    </tags>
</function>