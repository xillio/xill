<?xml version="1.0" encoding="utf-8"?>
<function>
    <description>
Returns the trimmed version of `string`.

If the optional boolean parameter `internal` is set to `true`, `String.trim()` will also replace slack white spaces - including tabs, non-breakable spaces and line feeds - inside `string` with a single space.
    </description>
    <examples>
        <example title="Usage">
            <code>
                use System, String;

                System.print(String.trim(" Xillio   "));
                // Result: "Xillio"

                System.print(String.trim(" Xillio     Xillio  ", true));
                // Result: "Xillio Xillio"
            </code>
        </example>
    </examples>
    <searchTags>
        whitespace
    </searchTags>
</function>