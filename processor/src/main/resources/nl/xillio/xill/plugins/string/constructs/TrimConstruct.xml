<?xml version="1.0" encoding="utf-8"?>
<function>
    <description>
Returns the trimmed version of the string.  

If the `internal` is set to `true`, the routine will also replace slack white spaces - including
tabs, non-breakable spaces and line feeds - inside the string with a single space.
    </description>
    <examples>
        <example title="Usage">
            <code>
                use String;

                String.trim(" Xillio   ");
                // Result: "Xillio"

                String.trim(" Xillio     Xillio  ", true);
                // Result: "Xillio Xillio"
            </code>
        </example>
    </examples>
    <searchTags>
        whitespace
    </searchTags>
</function>