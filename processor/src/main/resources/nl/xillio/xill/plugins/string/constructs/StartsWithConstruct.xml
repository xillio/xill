<?xml version="1.0" encoding="utf-8"?>
<function>
    <description>
Returns `true` if the `string` starts with `prefix`, or `false` if it does not.
    </description>
    <examples>
        <example title="Usage">
            <code>
                use System, String;

                if (String.startsWith("testing", "test")) {
                    System.print("testing starts with test");
                }
            </code>
        </example>
    </examples>
    <references>
        <reference>endsWith</reference>
    </references>
</function>