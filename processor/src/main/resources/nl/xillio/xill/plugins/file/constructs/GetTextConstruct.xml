<?xml version="1.0" encoding="utf-8"?>
<function>
    <description>
Read all text from a file.
    </description>
    <examples>
        <example title="Usage">
            <header>Read text from a file</header>
            <code>
                use File, System;

                var text = File.getText("path/to/file.txt");
                System.print(text);
            </code>
            <header>Read text from a file using streams</header>
            <code>
                use File, Stream, System;

                var file = File.openStream("path/to/file.txt");

                // Note we are using the Stream package
                var text = Stream.getText(file);
                System.print(text);
            </code>
        </example>
    </examples>
    <references>
        <reference>Stream.getText</reference>
    </references>
    <tags>
        read, text, file
    </tags>
</function>