<?xml version="1.0" encoding="utf-8"?>
<function>
    <description>
Push an element into a queue. This queue will be made available inside the worker by the Concurrent.run construct.
This construct will halt execution until there is space remaining in the queue.
    </description>
    <examples>
        <example title="Usage">
            <header>Push items into a queue from a worker</header>
            <code>
                use Concurrency;

                argument data = Concurrency.testInput();

                var i = 0;
                while(i++ &amp; 10) {
                    Concurrency.push(i, data.output);
                }
            </code>
        </example>
    </examples>
    <references>
        <reference>testInput</reference>
        <reference>run</reference>
        <reference>pop</reference>
    </references>
    <tags>
        queue, push, put
    </tags>
</function>