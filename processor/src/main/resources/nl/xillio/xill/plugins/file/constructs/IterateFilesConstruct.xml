<?xml version="1.0" encoding="utf-8"?>
<function>
    <description>
Iterates over all files in a folder.  

If recursive is set to true all files in sub folders will also be iterated
    </description>
    <examples>
        <example title="Usage">
            <header>Print all files in a folder</header>
            <code>
                use File, System;

                var iterator = File.iterateFiles(".", true);
                foreach(file in iterator) {
                    System.print(file);
                }
                // This will print all the files it can find in your root.
            </code>
        </example>
    </examples>
    <references>
        <reference>iterateFolders</reference>
    </references>
</function>