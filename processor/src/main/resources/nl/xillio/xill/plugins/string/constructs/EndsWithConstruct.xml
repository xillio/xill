<?xml version="1.0" encoding="utf-8"?>
<function>
    <description>
Returns `true` if `string1` ends with `string2` or `false` if it does not.
    </description>
    <examples>
        <example title="Usage">
            <code>
                use System, String;

                if (String.endsWith("testing", "ing")) {
                    System.print("testing ends with ing");
                }
            </code>
        </example>
    </examples>
    <references>
        <reference>startsWith</reference>
    </references>
</function>