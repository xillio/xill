<?xml version="1.0" encoding="utf-8"?>
<function>
    <description>
Returns the current version of the language.  

If a required version is provided it will throw an error when an older version is used, else it will return the current version.
    </description>
    <examples>
        <example title="Usage">
            <code>
                use System;

                System.print(System.version());
                // Result: 3.*.*

                System.print(System.version("1984.0.0"));
                // Throws an error and outputs: Version 1984.0.0 is not supported in 3.*.*
            </code>
        </example>
    </examples>
    <searchTags>
        version, check, error
    </searchTags>
</function>