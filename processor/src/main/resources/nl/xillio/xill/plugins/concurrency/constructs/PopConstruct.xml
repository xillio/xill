<?xml version="1.0" encoding="utf-8"?>
<function>
    <description>
Take an element from a queue. This queue will be made available inside the worker by the Concurrent.run construct.
This construct will halt execution until an element is present in the queue or it has been closed.
    </description>
    <examples>
        <example title="Usage">
            <header>Pop items from a queue from a worker</header>
            <code>
                use Concurrency;

                argument data = Concurrency.testInput();

                var item = Concurrency.pop(data.input);
                while(item != null) {
                    // Do something with the item

                    item = Concurrency.pop(data.input);
                }
            </code>
        </example>
    </examples>
    <references>
        <reference>testInput</reference>
        <reference>run</reference>
        <reference>push</reference>
    </references>
    <tags>
        queue, pop, take
    </tags>
</function>