<?xml version="1.0" encoding="utf-8"?>
<function>
    <description>
Creates a date variable from provided datetime string. Returns new date variable.  

If no parameters are passed, Date.now() is used.
The default format for string date values is ISO.  
Optionally a different format can be passed as second parameter.

For other format options see the Date.format help page.
    </description>
    <examples>
        <example title="Usage">
            <header>Parsing date and time.</header>
            <code>
use System;
use Date;
var date = Date.parse("1986-04-08 12:30", "yyyy-MM-dd HH:mm");
System.print(date);
            </code>
        </example>
    </examples>
    <references>
        <reference>now</reference>
        <reference>of</reference>
        <reference>format</reference>
    </references>
	    <tags>
        date, time, parse, datetime, timezone, now
    </tags>
</function>