<?xml version="1.0" encoding="utf-8"?>
<function>
    <description>
Wraps `text`, identifying words by a space character (" ").

If `wrapLongWords` is `true`, then words are split as well.
`width` specifies the number of characters after which the line is wrapped.
Leading spaces on a new line are stripped. Trailing spaces are not stripped.
    </description>
    <examples>
        <example description="Usage">
            <code>
                use System, String;

                var test = "Time is an illusion. Lunchtime doubly so.";
                System.print(String.wrap(test, 10, false));
                /*
                    Outputs:

                    Time is an
                    illusion.
                    Lunchtime
                    doubly so.
                */
            </code>
        </example>
    </examples>
    <searchTags>
        format, wrap
    </searchTags>
</function>