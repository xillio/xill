<?xml version="1.0" encoding="utf-8"?>
<function>
    <description>
Wraps a single line of text, identifying words by a whitespace.

If wrapLongWords is true then words are split as well.  
Leading spaces on a new line are stripped. Trailing spaces are not stripped.
    </description>
    <examples>
        <example description="Usage">
            <code>
                use System, String;

                var test = "Time is an illusion. Lunchtime doubly so.";
                System.print(String.wrap(test, 10, false));
                /*
                    Outputs:

                    Time is an
                    illusion.
                    Lunchtime
                    doubly so.
                */
            </code>
        </example>
    </examples>
    <searchTags>
        format, wrap
    </searchTags>
</function>