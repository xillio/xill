<?xml version="1.0" encoding="utf-8"?>
<function>
    <description>
Wraps a single line of text, identifying words by ' '.  

If wrapLongWords is true then words are split as well.  
Leading spaces on a new line are stripped. Trailing spaces are not stripped.
    </description>
    <examples>
        <example description="Usage">
            <code>
                use String;

                var test = "Time is an illusion. Lunchtime doubly so.";
                String.wrap(test, 10, false);
                /*
                    Outputs:

                    Time is an
                    illusion.
                    Lunchtime
                    doubly so.
                */
            </code>
        </example>
    </examples>
    <searchTags>
        format, wrap
    </searchTags>
</function>